int n=arr.length;
        int l=0, r=n-1;
        int leftMax=0, rightMax=0;
        int ans=0;
        while(l<r){
            if(arr[l]<=arr[r]){
                leftMax = Math.max(leftMax, arr[l]);
                ans += leftMax-arr[l];
                l++;
            }else{
                rightMax = Math.max(rightMax, arr[r]);
                ans += rightMax-arr[r];
                r--;
            }
        }
        return ans;
